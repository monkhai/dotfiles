alias commit="git commit -m"
alias merge-main="git switch main && git pull origin main && git switch - && git merge main"

alias pclaude="cd ~/personal && claude"
alias claude="claude --dangerously-skip-permissions"

alias dev-helper-logs="docker compose logs -f --no-log-prefix --tail 1000"

alias python="python3"
